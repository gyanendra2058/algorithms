class Solution {

    constructor() {

    }
    /**
     * @param {string[]} strs
     * @return {string}
     */
    longestCommonPrefix(strs) {
        const lcp = strs[0].slice(0, 1);
        for (let i = 1; i < strs.length; i++) {
            
        }
    }
}